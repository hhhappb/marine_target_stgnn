# Training Logs

Training and evaluation runs should write logs under:

```text
logs/training/<run_id>/
```

Use `YYYYMMDD-HHMMSS_<experiment_name>` for `run_id` when possible. Keep this README tracked; generated run directories are ignored by git.
