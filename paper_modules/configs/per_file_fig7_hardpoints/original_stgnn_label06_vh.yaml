paths:
  data_dir: datasets/ipix_dartmouth/processed/window4_stride4_related_stats_train_only
  save_dir: logs/training/per_file_fig7_hardpoints/original_stgnn_label06_vh

dataset:
  type: ipix_window
  data_dir: datasets/ipix_dartmouth/processed/window4_stride4_related_stats_train_only
  sources: [19931109_191449_starea]
  polarizations: [vh]

ipix:
  polarizations: [vh]

model:
  name: original_stgnn
  pulses: 4
  range_cells: 14

loss:
  type: cross_entropy

train:
  epochs: 50
  batch_size: 512
  learning_rate: 0.001
  num_workers: 0
  seed: 42

eval:
  protocol: per_file_pol
  threshold_source: train_clutter
  pfa_values: [0.001]
